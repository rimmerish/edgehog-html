// Leave blank for local-only scores.
// Use "same-origin" when /api/scores is routed through the same domain.
// Or set a full Worker URL, for example: "https://mirror-beam-scores.your-name.workers.dev"
window.MIRROR_BEAM_SCORE_API = "";
